{% if is_granted('ROLE_APP_OFFER_HIDE') %}
    <button
            type="button"
            class="btn btn-sm btn-default"
            data-toggle="modal"
            data-target="#modal-remove-record"
            data-url="{{ path('app_offer_hide', { id: offer.id}) }}"
    >
        <i class="fa fa-trash-o"></i>
        Удалить
    </button>
{% endif %}